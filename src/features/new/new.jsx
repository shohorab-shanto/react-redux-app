import React from 'react'
import { useSelector } from 'react-redux'

const Masakasa = () => {
    // const user = useSelector((state) =>console.log(state));
  return (
    <div>Masakasa</div>
  )
}

export default Masakasa;